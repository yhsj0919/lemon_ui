# 项目协作规则

## 主题与设备尺寸

- 主题、设备解析和组件尺寸实现必须遵守 [主题与尺寸架构](docs/theme-architecture.md)。
- 所有手机、平板、桌面和手表尺寸统一由主题系统管理；组件内部不得保存或通过 `switch` 选择各端尺寸常量。
- 设备类型表示稳定的真实设备类别，不表示页面形态；Windows、Linux 和 macOS 固定为 desktop，普通窗口缩放不得把设备重新分类为 phone、tablet 或 watch。
- 全局主题负责多端规格，局部主题和实例 Style 只覆盖当前解析结果；系统级自定义统一使用强类型 `copyWith`。
- 主题类随组件增加而变大是集中式强类型设计的预期成本，不得因此改用字符串 Map、动态注册表或分散的组件设备常量。
- 组件行为和接口稳定后，主动将属于组件职责的可配置视觉、排版和尺寸项纳入强类型主题模板：四端默认尺寸进入 `HyperSizeScheme`，全局视觉配置进入对应组件 `ThemeData`，局部与实例通过 `Style` 覆盖。同步补齐 `copyWith`、插值、值相等、Demo 和主题覆盖测试；可由父布局直接组合的外边距等冗余选项不进入组件主题。
- 新组件默认外观遵守 [HyperOS 风格设计基准](docs/hyperos-design-baseline.md)：手机端先核实同代 HyperOS 官方资料与目标截图；desktop 端的尺寸、排版、间距、圆角、颜色和交互状态以小米 HIUI 设计指南和源码为主要参考。第三方 MIUIX 只作手机端候选。缺少直接参考时从同端已确认的语义规格推导，并记录来源级别、单位和待验证项。不得用整齐数字、物理像素或跨端倍率代替证据。

## 动画与状态切换

- 有状态变化的控件应提供连续、克制的默认过渡，避免内容、尺寸或图标突然跳变；时长与曲线优先取全局 Motion 主题，并遵守系统减少动画设置。
- 默认过渡的视觉效果应能通过公开的强类型接口替换；动效实现不应改变组件的数据、交互和布局职责。稳定后将可配置动效纳入组件主题模板，局部主题与实例 Style 只覆盖显式字段。

## 根因与组件职责

- 修复缺陷前必须先确认问题的实际来源和组件责任边界，不能因为现象出现在某个控件中，就默认修改该控件。
- 如果缺陷由子控件、主题、字体、布局约束、数据状态或公共基础设施产生，应在根因所属位置修复，不得在表现问题的控件上持续增加偏移、特判、重复包装或兼容补丁。
- 修改前应使用最小复现或对照案例验证根因；测试应覆盖根因和跨组件组合，而不只验证表面现象。
- 只有当行为确实属于当前控件职责时，才在当前控件中实现修复。若必须临时规避，应明确记录原因、影响范围和后续清理条件。
- 完成修复后清理试探性代码、失效补丁和重复路径，保持组件结构单一且职责清晰。

## 截图还原

- 组件默认行为先核对 Flutter 约束与交互标准；视觉值遵守 [HyperOS 风格设计基准](docs/hyperos-design-baseline.md)的来源分级，先核实适用的官方规格和目标设备，再把 MIUIX 源码作为候选。截图用于检验同状态视觉效果，不能代替已知单位的规格来源。
- 用户提供目标截图时，先确认截图的像素尺寸、显示缩放、设备逻辑尺寸和可测量的文字、颜色、边距、圆角及布局位置；分辨率或像素密度不可靠时，不得用物理像素差反推 dp、字号或设备类型。
- 实现后使用相同尺寸和状态渲染对照图，逐项比较目标与结果并继续修正；未经对照验证，不得宣称已精确还原或“基本吻合”。
- 截图无法确定的字体、资源、设备缩放或动态状态应明确标出，不能把推测当作已验证事实。
